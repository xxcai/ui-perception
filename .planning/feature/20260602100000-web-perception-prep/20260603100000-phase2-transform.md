# Phase 2: Web Transform + 格式统一

## 状态

已完成

## 背景

Phase 1 已完成：Web capture 链路跑通，JS 注入 ARIA tree → JSON 返回正确。
Phase 2 目标：实现 transform（JSON → YAML），同时统一 native/web 输出格式。

## 设计决策

### 1. 共享类型下沉 perception-core ✅

Native/Web transform 共用的语义模型从 native-plugin 下沉到 perception-core，消除 web-plugin 对 native-plugin 的依赖。

| 旧名（native-plugin） | 新名（perception-core） |
|---|---|
| NativeSemanticNode | SemanticNode |
| NativeSemanticRole | SemanticRole |
| NativeBounds | Bounds |
| NativeRoleDecision | RoleDecision |
| NativeSnapshotRenderer | SnapshotRenderer |
| NativeSnapshotRenderOptions | SnapshotRenderOptions |
| NativeRefAssigner | RefAssigner（prefix 参数化） |
| NativeSemanticStates | SemanticStates |

### 2. States 格式统一 ✅

统一为 key=value 格式（对齐 web，模型基于 web 训练数据更多）。

| 旧格式 | 新格式 | 说明 |
|---|---|---|
| `clickable` | `clickable` | 不变 |
| `clickable-inferred` | `clickable=inferred` | key=value |
| `clickable-guessed` | `clickable=guessed` | key=value，不再 alias 成 inferred |
| Native input text → name | text → `value=xxx` state | 对齐 web |

### 3. Native input hint/value 改造 ✅

- ViewHierarchyDumper 新增 hint 提取
- NativeViewNode 新增 `hint` 字段
- INPUT 角色 name 取 hint（> contentDescription > resourceId），text → value=xxx state

### 4. Web transform 链路 ✅

```
JSON (JS capture 输出)
  → WebJsonParser.parse()        [JSON → SemanticNode 树]
  → TreeNormalizer.normalize()   [generic 折叠 + 冗余文本去除]
  → RefAssigner.assign(root, "w") [分配 w1/w2/...]
  → SnapshotRenderer.render()     [SemanticNode → YAML]
```

### 5. Ref 分配统一 ✅

RefAssigner + prefix 参数化，native 用 `"n"`，web 用 `"w"`。

## 完成步骤

### 阶段一：基础设施 ✅

1. ✅ 共享类型下沉 perception-core
2. ✅ States 格式更新（clickable=inferred/guessed）
3. ✅ 编译通过 + native 测试通过

### 阶段二：Native input 改造 ✅

4. ✅ ViewHierarchyDumper 新增 hint 提取
5. ✅ NativeViewNode 新增 hint 字段
6. ✅ INPUT 角色调整 name 来源，text → value=xxx state
7. ✅ native 测试通过

### 阶段三：Web transform ✅

8. ✅ WebJsonParser（JSON → SemanticNode）
9. ✅ TreeNormalizer（generic 折叠 + 冗余文本去除）
10. ✅ WebPerceptionPlugin.transform() 接入
11. ✅ 端到端验证（/capture-raw → web YAML 输出正确）

### 阶段四：验证（待 Phase 3 融合后完成）

12. 待 Phase 3 完成后做整体回归

## 关键文件

| 操作 | 文件 |
|---|---|
| 移动+重命名 | native-plugin/.../semantic/* → perception-core/.../semantic/* |
| 修改 | perception-core/.../semantic/SemanticStates.java — clickable=inferred/guessed |
| 修改 | perception-core/.../semantic/SnapshotRenderer.java — 移除 guessed→inferred aliasing |
| 修改 | perception-core/.../semantic/RefAssigner.java — prefix 参数化 |
| 修改 | native-plugin/.../ViewHierarchyDumper.java — hint 提取 |
| 修改 | native-plugin/.../semantic/NativeViewNode.java — hint 字段 |
| 修改 | native-plugin/.../semantic/NativeRoleResolver.java — INPUT name 来源调整 |
| 修改 | native-plugin/.../semantic/NativeSemanticTreeBuilder.java — INPUT value=xxx state |
| 新建 | web-plugin/.../WebJsonParser.java — JSON → SemanticNode |
| 新建 | web-plugin/.../TreeNormalizer.java — generic 折叠 + 冗余文本去除 |
| 修改 | web-plugin/.../WebPerceptionPlugin.java — transform() 接入 |
